# ADAN

Repositorio unificado del proyecto ADAN.

## Estructura

| Carpeta | Origen | Descripción |
|---|---|---|
| `autonomous/` | `hernanjrestrepo/adan_autonomous` (último commit original `2fab0bc`, oct 2024) | Prototipo de agente autónomo en AWS: Lambda que crea clones de sí misma (`lambda_function.py`), código autogenerado a partir de instrucciones (`autogenerated_script.py`) y despliegue con CodePipeline/CodeBuild (`buildspec.yml`). |

`hernanjrestrepo/ADAN-BACK` estaba vacío (sin commits), así que no aportó contenido.

El historial de `adan_autonomous` se conservó completo: los commits originales se reescribieron para vivir bajo `autonomous/`, con sus autores y fechas intactos (`git log -- autonomous/`).

## Despliegue de `autonomous/`

Si se conecta un proyecto de CodeBuild a este repositorio, configura la ruta del buildspec como `autonomous/buildspec.yml`. CodeBuild ejecuta los comandos desde la raíz del repo, por eso las pruebas se invocan con `pytest autonomous/tests/`.
